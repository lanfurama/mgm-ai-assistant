# Server
PORT=8000
NODE_ENV=development

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mgm_ai_assistant
DB_USER=postgres
DB_PASSWORD=your_password

# CORS Configuration
# For reverse proxy setups: Set to '*' to allow all origins
# For specific domain: Set to 'https://your-domain.com'
# For multiple domains: Set to 'https://domain1.com,https://domain2.com'
# For development: Set to 'http://localhost:3004' (or your frontend port)
FRONTEND_URL=http://localhost:3004
